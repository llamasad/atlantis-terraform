version: 3
projects:
  - dir: .
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
    apply_requirements: [approved]
    workflow: custom-workflow

workflows:
  custom-workflow:
    plan:
      steps:
        - init
        - plan
    apply:
      steps:
        - apply
